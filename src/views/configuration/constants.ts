export enum DOMAN_STATUS_ENUM {
  sync = 'SYNCHRONIZED',
  notSync = 'NOT SYNCHRONIZE',
  notFound = 'NOT FOUND',
}

export enum DOMAN_STATUS_LABEL_ENUM {
  'SYNCHRONIZED' = '已同步',
  'NOT SYNCHRONIZE' = '未同步',
  'NOT FOUND' = '未知状态',
}
